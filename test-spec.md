# テスト仕様書：ことば合わせ（Flask + Vue.js + Gemini / Ollama）

## 1. テスト対象

- `app.py`（Flaskサーバー・API・ゲームロジック）
- `static/index.html`（Vue.jsフロントエンドUI）
- Gemini API
- Ollama（Gemini利用不可時のフォールバック）

## 2. テスト環境

- Windows 10/11
- Python 3.8以降
- Flask
- `google-genai`
- `httpx`
- `python-dotenv`
- Google Gemini APIキー設定済み
- Ollamaおよび `qwen2.5:1.5b` が利用可能（フォールバックテスト時）
- ブラウザ：Chrome、Edge、Firefox

## 3. アプリ概要

「ことば合わせ」は、入力したテーマに対してAIが8組の問題ペアを生成し、それらを16枚のカードとして遊ぶ神経衰弱形式のWebアプリである。

- 1テーマにつき8個の異なるanswerを生成する。
- 1つのanswerにつき、異なる表現・聞き方の問題を2問生成する。
- 合計8ペア、16枚のカードを生成する。
- カードには問題文のみを表示し、answerはゲーム開始時にはフロントエンドへ公開しない。
- 2枚のカードを選択すると、バックエンドでanswerを比較してペア成立を判定する。
- ペア成立時にはバックエンドからanswerを返す。
- 全8ペア成立時にスコアを計算し、プレイ履歴へ保存する。
- 同じテーマで再生成した場合、過去に使用したanswerをできるだけ避ける。
- Geminiが利用できない場合はOllamaへフォールバックする。

## 4. テスト項目

### 4.1 サーバー起動・基本UI

| No | テスト内容 | 手順 | 期待結果 |
|---|---|---|---|
| 1 | サーバー起動 | `python app.py`で起動 | エラーなく起動し、`http://localhost:5000/`へアクセスできる |
| 2 | トップページ表示 | `/`へアクセス | 「ことば合わせ」のUIが表示される |
| 3 | テーマ入力 | テーマ欄へ文字列を入力 | 入力内容が表示される |
| 4 | テーマ未入力 | テーマを空欄にする | 「ゲームを始める」ボタンを実行できない |
| 5 | テーマ上限 | 51文字以上のテーマをAPIへ送信 | 「お題は50文字以内で入力してください。」が返る |

### 4.2 `/generate_game` 正常系

| No | テスト内容 | 手順 | 期待結果 |
|---|---|---|---|
| 6 | ゲーム生成 | 有効なテーマを送信 | HTTP 200で`game_id`と16枚のカードが返る |
| 7 | カード枚数 | 生成結果を確認 | カードが16枚ある |
| 8 | カードID | 生成結果を確認 | 1～16のカードIDが設定される |
| 9 | 問題文表示 | ゲームを開始 | カードには問題文のみ表示される |
| 10 | answer非公開 | `/generate_game`のレスポンスを確認 | カードのレスポンスにanswerが含まれない |
| 11 | ペア構成 | 16枚のカードを確認 | 同じanswerを持つカードが2枚ずつ、8ペア存在する |
| 12 | カードシャッフル | 複数回ゲームを生成 | カードの並び順がランダム化される |

### 4.3 AIによるゲームデータ生成

| No | テスト内容 | 手順 | 期待結果 |
|---|---|---|---|
| 13 | Gemini正常生成 | Geminiが利用可能な状態でゲーム開始 | Geminiによってゲームデータが生成される |
| 14 | 8ペア生成 | 生成データを確認 | `pairs`が8個ある |
| 15 | 各ペアの問題数 | 各pairを確認 | `questions`が必ず2個ある |
| 16 | answer重複 | 生成データを確認 | 8個のanswerがすべて異なる |
| 17 | 問題文重複 | 各pairを確認 | 2つの問題文が同一ではない |
| 18 | answerの問題文への混入 | 生成データを確認 | questionsの文中にanswerそのものが含まれない |
| 19 | 個人的質問の除外 | 生成データを確認 | 個人的な問いかけが含まれない |
| 20 | テーマとの関連性 | 複数テーマで生成 | テーマから大きく外れた問題が生成されない |

### 4.4 Geminiモデルのフォールバック

Geminiは次の優先順位で使用する。

1. `gemini-3.6-flash`
2. `gemini-3.5-flash-lite`
3. `gemini-3.1-flash-lite`
4. すべてのGeminiが利用できない場合はOllama

| No | テスト内容 | 手順 | 期待結果 |
|---|---|---|---|
| 21 | 3.6正常 | `gemini-3.6-flash`が正常な状態で生成 | 3.6で成功し、下位モデルを呼び出さない |
| 22 | 3.6の503 | 3.6で503を発生させる | 3.6を1回再試行後、3.5へ切り替える |
| 23 | 3.6の429 | 3.6で429を発生させる | 同じモデルを繰り返さず、3.5へ切り替える |
| 24 | 3.6・3.5失敗 | 両モデルを利用不可にする | 3.1へ切り替える |
| 25 | 全Gemini失敗 | 3モデルすべてを利用不可にする | Ollamaへフォールバックする |
| 26 | APIキー未設定 | `GEMINI_API_KEY`を設定せず起動 | 最初からOllamaを使用する |
| 27 | フォールバックログ | 各モデルで失敗を発生させる | 使用モデルと切り替え理由がログに記録される |

### 4.5 Ollamaフォールバック

| No | テスト内容 | 手順 | 期待結果 |
|---|---|---|---|
| 28 | Ollama接続 | Geminiを利用不可にしてゲーム開始 | `qwen2.5:1.5b`で生成を試行する |
| 29 | answer候補生成 | Ollamaフォールバックを実行 | 複数のanswer候補から有効な候補を収集する |
| 30 | answer重複除外 | 重複answerを含む出力を生成 | 重複answerが採用されない |
| 31 | 過去answer除外 | 同じテーマで再度ゲーム生成 | 過去に使用したanswerができるだけ採用されない |
| 32 | 問題候補生成 | 有効なanswerについて問題候補を生成 | answerを含まない問題候補を生成する |
| 33 | 問題文検証 | 不正な問題候補を生成 | answer混入、個人的表現、疑問文でない問題などが除外される |
| 34 | 8ペア未達 | Ollamaで8ペアを作れない状態にする | ゲームを開始せず生成エラーになる |

### 4.6 `/check_pair` 正常系

| No | テスト内容 | 手順 | 期待結果 |
|---|---|---|---|
| 35 | ペア成立 | 同じanswerの2枚を選択 | `is_pair: true`が返る |
| 36 | answer返却 | ペア成立時のレスポンスを確認 | バックエンドから該当answerが返る |
| 37 | 不成立 | 異なるanswerの2枚を選択 | `is_pair: false`が返る |
| 38 | 手数加算 | カードを2枚選択して判定 | 1回の判定ごとに`moves`が1増える |
| 39 | 全ペア成立 | 8ペアすべてを成立させる | `game_cleared: true`になる |
| 40 | スコア計算 | 全ペア成立後に確認 | `moves`に応じたスコアが返る |
| 41 | プレイ履歴保存 | ゲームクリア後に履歴を確認 | テーマ、手数、スコア、経過時間などが保存される |

### 4.7 `/check_pair` 異常系

| No | テスト内容 | 手順 | 期待結果 |
|---|---|---|---|
| 42 | 存在しないgame_id | 無効な`game_id`を送信 | HTTP 400で「ゲームが見つかりません。」 |
| 43 | card_ids不足 | 1枚だけ指定 | HTTP 400で2つのカードIDが必要というエラー |
| 44 | card_ids過多 | 3枚指定 | HTTP 400で2つのカードIDが必要というエラー |
| 45 | 無効なカードID | 存在しないIDを指定 | HTTP 400で無効なカードIDエラー |
| 46 | 同一カード | 同じIDを2つ指定 | HTTP 400で同じカードを選択できない |
| 47 | 成立済みカード | すでにペア成立したカードを再指定 | HTTP 400で再選択できない |
| 48 | クリア後の操作 | クリア済みゲームへリクエスト | HTTP 400でゲーム終了エラー |

### 4.8 スコア

現在のスコア計算は以下である。

```python
max(0, 1000 - (moves - 10) * 30)
```

| No | テスト内容 | 手順 | 期待結果 |
|---|---|---|---|
| 49 | 最少手数 | 8手でクリア | スコア1000点 |
| 50 | 追加手数 | 8手より多い手数でクリア | 1手増えるごとに10点減少する |
| 51 | 下限確認 | 非常に多い手数でクリア | スコアが0点未満にならず0点になる |
| 52 | クリア前 | ゲーム途中で確認 | 最終スコアはまだ確定しない |

### 4.9 `/game_history`

| No | テスト内容 | 手順 | 期待結果 |
|---|---|---|---|
| 53 | 履歴取得 | `GET /game_history` | JSON形式で履歴が返る |
| 54 | 履歴順序 | 複数ゲームをクリアして確認 | 新しい履歴が先に表示される |
| 55 | 履歴件数上限 | 51件以上ゲームをクリア | 最新50件のみ保持される |
| 56 | 履歴表示 | ブラウザで確認 | テーマ、手数、スコアなどが表示される |

### 4.10 `/quiz_history`

| No | テスト内容 | 手順 | 期待結果 |
|---|---|---|---|
| 57 | クイズ履歴取得 | `GET /quiz_history` | 生成済みクイズの履歴がJSONで返る |
| 58 | 履歴件数上限 | 101件以上生成 | 最新100件のみ保持される |
| 59 | 同一テーマ再生成 | 同じテーマで複数回生成 | 過去使用answerを考慮して新しいanswerが生成される |

### 4.11 UI・ゲーム操作

| No | テスト内容 | 手順 | 期待結果 |
|---|---|---|---|
| 60 | カードを1枚選択 | 裏向きカードをクリック | 問題文が表示される |
| 61 | 2枚選択 | 2枚のカードをクリック | 2枚とも問題文が表示され、ペア判定が行われる |
| 62 | 不成立時 | 異なるペアを選択 | 約0.8秒後に2枚が裏返る |
| 63 | 成立時 | 正しいペアを選択 | ペア成立としてカードが固定される |
| 64 | 成立カード再操作 | 成立済みカードをクリック | 再度選択できない |
| 65 | ペア数表示 | ペアを成立させる | `ペア n / 8`の表示が増える |
| 66 | 手数表示 | ペア判定を繰り返す | 手数が正しく表示される |
| 67 | ゲームクリア表示 | 8ペアすべて成立 | 「ゲームクリア」とスコアが表示される |
| 68 | もう一度遊ぶ | クリア後に「もう一度遊ぶ」 | 同じテーマで新しいゲームを生成できる |
| 69 | 生成中UI | ゲーム生成中に確認 | ボタンが無効化され「カードを準備中...」と表示される |
| 70 | エラー表示 | ゲーム生成に失敗 | UI上にエラーメッセージが表示される |

## 5. セキュリティ・データ公開確認

| No | テスト内容 | 手順 | 期待結果 |
|---|---|---|---|
| 71 | APIキー非公開 | ブラウザのレスポンスを確認 | `GEMINI_API_KEY`がフロントエンドへ送信されない |
| 72 | answer非公開 | `/generate_game`レスポンスを確認 | ゲーム開始時のレスポンスにanswerが含まれない |
| 73 | answerのサーバー保持 | `/generate_game`後にペア判定 | answerはバックエンドのゲーム状態を使って判定される |
| 74 | ゲーム状態保持 | 同じ`game_id`で複数回判定 | 各ゲームのカード・成立状態・手数が維持される |

## 6. ゲームデータ検証ルール

生成されたゲームデータは、以下を満たさない場合は不正データとして扱う。

- `pairs`が8個である。
- `pair_id`が1～8の整数で重複しない。
- answerが空でない。
- 8個のanswerが重複しない。
- 各pairのquestionsが2個である。
- questionsが空でない。
- 2つのquestionsが同一ではない。
- questionsの文中にanswerそのものを含まない。
- 個人的な表現を含まない。
- Ollamaで生成した問題は疑問文として成立していることも確認する。

## 7. ゲーム終了・履歴

全16枚のカードが成立するとゲームクリアとなる。

クリア時には以下を履歴として保存する。

- game_id
- theme
- started_at
- cleared_at
- moves
- score
- elapsed_seconds

履歴はゲーム履歴について最大50件、クイズ生成履歴について最大100件保持する。

## 8. 追加確認項目

- 日本語のテーマで正常にゲーム生成できること。
- 「日本の歴史」「宇宙」「動物」など複数のテーマで正常に動作すること。
- テーマ入力の前後に空白があっても適切に処理されること。
- Geminiが一時的に利用できない場合でもOllamaへ切り替えられること。
- GeminiとOllamaのどちらも利用できない場合、ゲームを開始しないこと。
- 複数ブラウザでUIが大きく崩れないこと。
- レスポンス時間の目安を測定すること。
- Flask再起動後はメモリ上のゲーム状態・履歴が保持されないことを確認すること。
