# Webアプリ仕様書

アプリ名：ことば合わせ

## 1. 概要

LLM（大規模言語モデル）を利用してクイズを生成し、そのクイズを利用した神経衰弱ゲームを提供するWebアプリ。

プレイヤーは「日本の首都」「宇宙」「スポーツ」などのお題を入力する。入力されたお題をもとにLLMが関連する単語を8個生成し、さらに各単語について2つずつクイズを生成する。

各単語について生成された2つのクイズは、それぞれ異なる問題文でありながら同じ答えになるようにする。この2つのクイズを1組のペアとして扱い、合計8組、16枚のクイズカードを作成する。

例えば、お題として「日本の首都」が入力された場合、関連する単語として「東京」が生成される。その後、「東京」に関する以下のような2つのクイズを生成する。
- 「日本の首都はどこですか？」
- 「日本の政治の中心となっている都市はどこですか？」
- どちらの問題の答えも「東京」であるため、この2枚のカードが1組のペアとなる。
- カードには答えではなくクイズの問題文を表示する。プレイヤーはカードをめくって問題を読み、それぞれの答えを考えながら、同じ答えになる2枚のカードを探す。
- 通常の神経衰弱ではカードの絵柄や数字を記憶してペアを探すが、本アプリでは「問題文から答えを考え、その答えが同じカードを探す」という方式を採用する。これにより、LLMが生成する異なる表現のクイズをゲーム性として利用する。
- 1ゲームにつき8組、合計16枚のカードを使用する。

## ペルソナ

### ターゲットユーザー

高校生、大学生、社会人

### ペルソナ

- 授業の空き時間や帰宅後にスマートフォンやPCでゲームをする
- クイズやパズルなど、考えながら遊ぶゲームが好き
- 暗記だけをする勉強は苦手だが、ゲーム感覚なら楽しみながら知識を身につけたい
- 「日本の歴史」「動物」「スポーツ」「宇宙」など、興味のあるテーマについて遊びながら知識を確認したい
- 毎回同じ問題ではなく、遊ぶたびに異なる問題を楽しみたい

### 利用シーン


- 授業の空き時間や自宅での暇つぶしに、PCやスマートフォンからゲームを利用する。
- プレイヤーは「日本の歴史」「宇宙」「動物」など、自分が興味のあるお題を入力する。LLMによってお題に関連する問題が自動生成されるため、毎回異なる内容でゲームを楽しむことができる。
- カードを2枚めくった際には、表示された2つの問題からそれぞれの答えを考える。そして、答えの表現が異なっていても同じ意味を持つ場合には、それらを同じペアとして判断する。

### 解決したい課題

- 通常の神経衰弱はカードの位置を覚えることが中心で、繰り返し遊ぶと単調になりやすい。
- 興味のあるテーマについて、ゲーム感覚で知識を確認する機会が少ない。
- 同じ答えでも異なる表現が存在することを利用した、従来とは異なる神経衰弱ゲームを提供する。
- LLMによって毎回異なる問題や回答表現を生成することで、同じゲームを繰り返しても新しい問題に挑戦できるようにする。


## 2. 主な機能

### 2.1 お題入力機能
- ユーザーがゲームのテーマとなるお題を入力できる。
- 「日本の歴史」「動物」「宇宙」「スポーツ」など、自由にお題を指定できる。
- お題が入力されていない場合は、エラーメッセージを表示する。
- 入力されたお題をLLMによるゲームデータ生成に利用する。
### 2.2 クイズ生成API
- ユーザーが入力したお題をLLMに送信する。
- LLMがお題に関連する8個の単語を生成する。
- 生成された各単語について、異なる2つのクイズを生成する。
- 2つのクイズは異なる問題文でありながら、同じ答えを指すように生成する。
- 問題文の表現や聞き方にLLMによる揺らぎを持たせる。
- 生成されたクイズをJSON形式でバックエンドに返す。
### 2.3 カード生成・シャッフル機能
- 生成された8組のクイズから、合計16枚のカードを作成する。
- 1組につき2枚のカードを割り当てる。
- カードにはクイズの問題文を設定する。
- ゲーム開始時に16枚のカードをランダムにシャッフルする。
- ペア情報や答えはプレイヤーから確認できないようにする。
### 2.4 カード選択・問題表示機能
- ユーザーがカードをクリックして選択できる。
- 選択したカードの問題文を表示する。
- 1回のターンで2枚のカードを選択できる。
- 2枚のカードを選択すると、それぞれの問題について答えを考えることができる。
### 2.5 ペア判定機能
- 選択された2枚のカードが同じペアか判定する。
- ペア判定はバックエンドで行う。
- 同じ答えに対応するカードの場合、ペア成立とする。
- ペアが成立した場合は、2枚のカードを場に残す。
- ペアが成立しなかった場合は、カードを裏返して再び選択できるようにする。
### 2.6 ゲームクリア機能
- 8組すべてのペアが成立したことを確認する。
- すべてのペアが成立した場合、ゲームクリアとする。
- ゲームクリア時にクリアしたことをユーザーに表示する。
- ゲームを最初からやり直せるようにする。
### 2.7 エラー処理機能
- お題が入力されていない場合にエラーを表示する。
- Ollamaとの通信に失敗した場合にエラーを表示する。
- LLMから不正なデータが返された場合にゲームを開始しない。
- クイズが8組16枚にならない場合にエラーとして処理する。
- 不正なカードIDやゲームIDが送信された場合にエラーとして処理する。

## 以下は追加できたらする機能
### 2.8 ゲーム再生成機能
- 同じお題で新しいクイズを生成できる。
- LLMによって異なるクイズが生成されるため、同じお題でも繰り返し遊べる。
- 「もう一度遊ぶ」ボタンから新しいゲームを開始できる。

### 2.9 スコア・手数表示機能
- ゲーム中のカード選択回数を表示する。
- ゲームクリアまでにかかった時間を記録する。
- 手数や時間をもとにスコアを計算する。
- より少ない手数・短い時間でのクリアを目指せる。

### 2.10 難易度設定機能
- ゲーム開始時に難易度を選択できる。
- 難易度に応じてカード枚数やクイズの難しさを変更する。
- 初心者でも遊びやすい難易度から、難しい問題まで選択できる。


## 3. 使用技術

| 分類         | 技術・ライブラリ |
|--------------|------------------|
| フロントエンド | Vue.js / HTML5 + CSS3 |
| バックエンド | Python / Flask |
| 通信方式 | Fetch API / JSON |
| クラウドLLM | Google Gemini API |
| Geminiモデル | `gemini-3.6-flash` |
| ローカルLLM | Ollama |
| Ollamaモデル | `qwen3.5:0.8b` |
| HTTP通信 | httpx |
| 環境変数管理 | python-dotenv |

### 3.1 LLMの利用優先順位

通常はGemini APIを優先して使用する。Gemini APIが利用できない場合、または規定回数の試行で有効なゲームデータを生成できない場合は、Ollamaの `qwen3.5:0.8b` にフォールバックする。

```text
Gemini API
    ↓
失敗・不正データ
    ↓
最大3回再試行
    ↓
まだ失敗
    ↓
Ollama（qwen3.5:0.8b）
    ↓
最大3回再試行
    ↓
まだ失敗
    ↓
ゲーム生成エラー
```

Gemini APIキーが設定されていない場合は、Geminiを使用せずOllamaを使用する。

## 4. システム構成図

フロントエンド、Flaskによるバックエンド、Ollama上で動作するLLMの3つの主要コンポーネントで構成する。
```text
┌──────────────────────────────┐
│          ユーザー             │
│                              │
│  お題入力・カード選択・結果確認 │
└──────────────┬───────────────┘
               │
               │ HTTP
               ▼
┌──────────────────────────────┐
│       フロントエンド          │
│                              │
│  ・お題入力画面               │
│  ・16枚のカード表示            │
│  ・カード選択                 │
│  ・ペア判定結果表示            │
│  ・ゲームクリア表示            │
└──────────────┬───────────────┘
               │
               │ REST API
               ▼
┌──────────────────────────────┐
│        Flaskバックエンド       │
│                              │
│  /generate_game              │
│  /check_pair                 │
│                              │
│  ・ゲームデータ生成            │
│  ・LLM出力の検証               │
│  ・カード生成・シャッフル       │
│  ・ゲーム状態管理              │
│  ・ペア判定                   │
└───────────┬──────────┬───────┘
            │          │
            │          │ ゲーム状態
            │          ▼
            │   ┌───────────────┐
            │   │ サーバー上の状態 │
            │   │               │
            │   │ game_id       │
            │   │ card_id→answer│
            │   │ matched       │
            │   └───────────────┘
            │
            │ OpenAI互換API
            ▼
┌──────────────────┐       ┌──────────────────────┐
│    Gemini API    │       │       Ollama         │
│                  │       │                      │
│ gemini-3.6-flash │       │ qwen3.5:0.8b        │
│ 優先利用           │       │ フォールバック利用      │
└──────────────────┘       └──────────────────────┘
          │                         ▲
          └──── 失敗・不正データ ────┘
```
### 4.1 コンポーネント間の連携
フロントエンド → Flask

ユーザーが入力したお題や選択したカードIDをHTTPリクエストとしてFlaskに送信する。

Flask → Gemini API

`/generate_game` の処理において、ユーザーのお題をGeminiへ送信し、8個の単語および8組のクイズデータを生成する。

Gemini → Flask

Geminiから返されたデータをFlask側でJSON形式や内容を検証する。Geminiで有効なデータを取得できない場合はOllamaへ切り替える。

Flask → Ollama

Ollamaでは `qwen3.5:0.8b` を使用して同じ生成処理を行う。

Ollama → Flask

Ollamaから返されたJSONをFlask側で検証する。

どちらのLLMを利用した場合でも、単語数、重複、クイズ数、答えと単語の一致などの共通検証を行う。

Flask → フロントエンド

検証したゲームデータから16枚のカードを生成し、カードIDと問題文をフロントエンドへ返す。

答えとペア情報はフロントエンドには返さず、バックエンドで保持する。

フロントエンド → Flask

2枚のカードが選択された場合、game_idと2つのcard_idを/check_pairへ送信する。

Flask → フロントエンド

バックエンドで保持している答えを比較し、ペア成立・不成立をレスポンスとして返す。

## 5. 画面設計 ＆ 画面遷移図

### 5.1 画面設計

メイン画面では、以下の項目を表示する。

- システム名
- ゲームの説明
- お題入力欄
- ゲーム開始ボタン

ゲーム開始後は、以下を表示する。

- お題
- 16枚のクイズカード
- 選択中のカード
- ペア判定結果

カードには問題文のみを表示し、答えは表示しない。

ペア成立時は、成立したカードを場に残す。

ペア不成立時は、カードを裏返して再度選択できるようにする。

8組すべてのペアが成立した場合は、以下を表示する。

- ゲームクリア表示
- もう一度遊ぶボタン

### 5.2 画面遷移図
```text
[お題入力画面]
      ↓
[ゲーム開始]
      ↓
[クイズ生成]
      ↓
[ゲーム画面]
      ↓
[カードを1枚選択]
      ↓
[カードを2枚選択]
      ↓
[ペア判定]
      ↓
   ┌──┴──────────┐
   ↓             ↓
[ペア成立]    [ペア不成立]
   ↓             ↓
[カードを残す] [カードを裏返す]
   ↓             ↓
   └──────┬──────┘
          ↓
   [次のカード選択]
          ↓
   [すべてのペア成立？]
       ↓       ↓
      No      Yes
       ↓       ↓
[ゲーム画面] [ゲームクリア]
                 ↓
           [もう一度遊ぶ]
                 ↓
           [お題入力画面]

```
## 6. API仕様

### 6.1 クイズゲーム生成API

- URL：`/generate_game`
- Method：`POST`
- Content-Type：`application/json`

#### Request JSON
```text
{
  "theme": "日本の歴史"
}
```
Response JSON
```text
{
  "game_id": "abc123...",
  "cards": [
    {
      "id": 1,
      "text": "日本の首都はどこですか？"
    },
    {
      "id": 2,
      "text": "日本の政治の中心となっている都市はどこですか？"
    }
  ]
}
```
cardsには16枚のカードが格納される。

### 6.2ペア判定API仕様
URL：/check_pair
Method：POST
Content-Type：application/json
Request JSON
```text
{
  "game_id": "abc123...",
  "card_ids": [1, 2]
}
```
Response JSON（ペア成立）
```text
{
  "is_pair": true,
  "answer": "東京",
  "game_cleared": false
}
```
Response JSON（ペア不成立）
```text
{
  "is_pair": false,
  "answer": null,
  "game_cleared": false
}
```
Response JSON（ゲームクリア）
```text
{
  "is_pair": true,
  "answer": "東京",
  "game_cleared": true
}
```
- is_pair：2枚のカードが同じ答えに対応するペアかどうか
- answer：ペア成立時の答え。ペア不成立時はnull
- game_cleared：8組すべてのペアが成立した場合にtrue
### 6.3 エラー仕様

APIでエラーが発生した場合は、以下の形式でレスポンスを返す。

Error Response JSON
```text
{
  "error": "エラーメッセージ"
}
```
お題未入力
HTTP Status：400
```text
{
  "error": "お題を入力してください。"
}
```
ゲームが存在しない
HTTP Status：400
```text
{
  "error": "ゲームが見つかりません。"
}
```
不正なカードID
HTTP Status：400
```text
{
  "error": "無効なカードIDです。"
}
```
同じカードを選択
```text
HTTP Status：400
{
  "error": "同じカードは選択できません。"
}
```
すでにペアが成立しているカードを選択
HTTP Status：400
```text
{
  "error": "このカードはすでにペアが成立しています。"
}
```
Ollamaとの通信失敗
HTTP Status：500
```text
{
  "error": "AIサービスとの通信中にエラーが発生しました。"
}
```

ゲームデータ生成失敗
HTTP Status：500
```text
{
  "error": "ゲームデータの生成に失敗しました。"
}
```
### 6.4 LLMゲームデータ生成仕様

FlaskバックエンドからLLMへユーザーが入力したお題を送信し、まずお題に関連する8個の単語を生成する。

その後、生成された8個の単語について、それぞれを答えとする異なる2つの問題文を生成する。

#### 単語生成

LLMは以下の形式のJSONを生成する。

```json
{
  "words": ["東京", "大阪", "京都", "奈良", "北海道", "沖縄", "福岡", "広島"]
}
```

検証条件：

- 8個ちょうどであること
- すべて文字列であること
- 空文字・空白だけの文字列がないこと
- 重複がないこと

#### クイズ生成

LLMは以下の形式のJSONを生成する。

```json
{
  "pairs": [
    {
      "pair_id": 1,
      "answer": "東京",
      "questions": [
        "日本の首都はどこですか？",
        "日本の政治の中心となっている都市はどこですか？"
      ]
    }
  ]
}
```

`pairs` は8個とし、各pairには以下を設定する。

- `pair_id`：ペアを識別するID
- `answer`：2つの問題文に共通する答え
- `questions`：同じ答えになる2つの異なる問題文

検証条件：

- 8個のペアがあること
- `pair_id` が重複しないこと
- 各ペアの `answer` が空でないこと
- 各ペアに問題文が2つあること
- 問題文が空でないこと
- 2つの問題文が同一ではないこと
- 8個の答えが重複しないこと
- 8個の答えの集合と、先に生成した8個の単語の集合が完全一致すること

最後の条件により、単語として生成されていない答えを持つクイズがゲームに混入することを防ぐ。

#### LLMの試行

Gemini APIが設定されている場合はGeminiを最大3回試行する。

- API呼び出しに失敗した場合は再試行する。
- JSONや生成内容が検証条件を満たさない場合も再試行する。
- 3回の試行で有効なデータを取得できなければOllamaへ切り替える。

Ollamaでは `qwen3.5:0.8b` を使用し、最大3回試行する。

Gemini APIキーが設定されていない場合は、最初からOllamaを使用する。

Ollamaでも有効なデータを取得できなかった場合はゲームデータ生成エラーとして処理し、ゲームを開始しない。

```text
単語生成
   ↓
LLMからJSON取得
   ↓
単語を検証
   ├─ 成功 → クイズ生成
   └─ 失敗 → 再生成
                 ↓
              最大3回
                 ↓
          Gemini失敗ならOllama
   ↓
クイズ生成
   ↓
クイズを検証
   ├─ 成功 → カード生成
   └─ 失敗 → 再生成
                 ↓
              最大3回
                 ↓
          Gemini失敗ならOllama
```

カード生成に成功した場合、16枚のカードをランダムにシャッフルし、フロントエンドにはカードIDと問題文のみを返す。

カードの答えはバックエンドのゲーム状態に保持し、ペア判定に利用する。